# Issues & Status — FinTech Atlas

> Actionable gaps and current status (completed items are marked 🟢 rather than
> removed so the repository-level audit trail remains readable). Complements
> [GAP-ANALYSIS.md](./GAP-ANALYSIS.md) and the per-goal checklists.
> Last refreshed: **2026-08-13**. Repository-level static-v1 work is current through the India-focus pass (fee calculator INR mode + GST, Razorpay vs Stripe + Razorpay vs Cashfree articles/presets, homepage repositioning + Popular Comparisons, per-article sitemap lastmods, GitHub Actions uptime monitor, Payoneer fees India article, Payoneer profile copy repair, dedicated Razorpay fee calculator page, catalog-wide editorial copy sweep, T049/T050/T051/T055 link + breadcrumb structured-data wiring, India-first homepage provider directory + latest-guides strip, India payment glossary terms), the editorial recency sort (homepage/articles-index/RSS with array-order tiebreak + e2e order contract), the payroll/business-banking/stablecoin/P2P article cycle (Gusto vs ADP vs Paychex, Brex vs Relay, USDC vs bank wire, Cash App vs Venmo — orphan closure 29→37 of 42), the LatAm + open-banking article cycle and T015 named author (How to get paid from Latin America in India, Plaid vs India's Account Aggregator — orphan closure 37→40 of 42; 2 remain: apple-pay, braintree), PWA browser verification, the T009 ownership audit (4 stale statuses corrected, provenance-backed) + gateway fee comparison & Shopify India articles (orphan closure 40→42 of 42 — zero orphans), and the Upwork/Fiverr payout-guide cycle with the client-bundle split (command palette now imports a generated article-summary subset; JS budget 447,840 → 410,062 gzip), and the final Month-2 article cycle (plan items 9 & 12: receive-USD decision hub + PayPal vs Payoneer India guides, web-verified) with the Klarna market-cap correction, the per-page meta CSP migration (hashed per-page policies in every HTML document replacing a ~60KB catch-all header — host-agnostic, GitHub Pages included), and the CSP meta `frame-ancestors` removal (CSP3 ignores it in `<meta>` and Chrome's console error failed the `errors-in-console` LHCI gate; clickjacking protection ships via `X-Frame-Options: DENY` in `_headers`).

**Architecture context:** fully static Next.js 16 export (`output: "export"`) —
no server, database, auth, or live third-party data feeds.
Backend-heavy goals are deferred per [ADR-001](../adr/001-defer-backend-capabilities.md).

---

## Status legend

| Mark | Meaning |
|------|---------|
| 🔴 | Blocked / deferred on backend (see ADR-001) |
| 🟡 | Buildable on current static architecture; incomplete |
| 🟢 | Mostly implemented; verification / hardening remains |
| ⚠️ | Contradiction or consistency issue |

---

## 1. Release criteria (production gate)

| # | Gap | Status |
|---|-----|--------|
| R1 | All critical features meet their Definition of Done | Open |
| R2 | No unresolved critical or high-severity defects | Open |
| R3 | Automated tests for critical paths pass consistently on `origin/main` | 🟢 Full local gate green 2026-08-13: 337 Vitest, 80-test Playwright, coverage 92.3% stmts, lint (0 warnings), typecheck+scripts, build+artifact, `npm audit --production` 0, LHCI gauntlet (6 URLs, errors-in-console/categories/LCP/TBT/CLS all pass); merge/CI-on-origin evidence remains |
| R4 | Monitoring, alerting, logging, and backups are active | 🟡 Repo-level uptime workflow added (30-minute probes of homepage/tools/articles → GitHub issue alerts; needs `DEPLOYMENT_URL` + GitHub settings); host-level logging/backups remain operator tasks (ADR-001) |
| R5 | Security review is complete | 🟡 In-code security review complete (SECURITY_REVIEW.md: dependency audit 0, secrets clean, per-page CSP meta + host headers verified in artifact); live-host header/HTTPS verification + external pentest remain operator items |
| R6 | Performance targets are met (live-host evidence) | 🟡 LHCI budget gate green on the static artifact (LCP ≤4500ms, TBT ≤300ms, CLS ≤0.1, perf/a11y/bp/seo categories); measurement against a real origin remains operator |
| R7 | Documentation is current | 🟢 Refreshed 2026-08-13: README counts (6 tools, 29 terms), security docs now describe the per-page meta CSP, runbook e2e count 80/80, ISSUES-AND-GAPS refreshed; jurisdiction-specific legal review remains operator (R9) |
| R8 | Deployment process is repeatable and supports verified rollback | 🟡 Docs exist (deployment-providers, incident-runbook); drill against the chosen host remains operator |
| R9 | Jurisdiction-specific legal review before launch | Open |
| R10 | E2E journey with registration | 🔴 Deferred (ADR-001) |
| R11 | v1 journey: calculator → persist → export/share → return | 🟢 Fee/remittance/matchmaker tools support save/share/CSV; e2e covers matchmaker flow + bookmarks persistence |

---

## 2. Deferred backend goals (🔴 — ADR-001)

Do not implement without revisiting [ADR-001](../adr/001-defer-backend-capabilities.md):

- Goal 01 Identity & auth
- Goal 03 AI assistant
- Goal 04 live market data / ETF / statements / APIs
- Goal 05 Portfolio tracker (server persistence)
- Goal 06 Personal finance ledgers (server persistence)
- Goal 11 Full observability stack
- Goal 13 Account-based product analytics (retention/churn)

---

## 3. Remaining product / tool gaps

| Gap | Status |
|-----|--------|
| PDF export for calculators | 🟢 Implemented via browser print dialog |
| Share / save / CSV parity on remittance + matchmaker | 🟢 Implemented; verify in browser/E2E |
| Typo-tolerant search beyond current fuzzy scoring | 🟢 Implemented for current static catalog; market-data scope deferred |
| Stocks / ETFs / funds / articles search content | 🔴 Deferred with Goal 04/07 content |
| Production `SITE_URL` set on real host | Operator action |
| Indexability / CWV / structured-data verification on live URLs | Open |
| Host applies `_headers` + serves `security.txt` | Operator action |
| Penetration test | Open |
| Preview environments + CD / deploy workflow | Open |
| Rollback / backup restoration drill | Open |
| Manual screen-reader audit | 🟡 Keyboard + focus-visibility + skip-link automated (WCAG 2.1.1/2.4.1/2.4.7 gate, 10 tests); CountUp now announces only the final value (sr-only twin, animated digits aria-hidden); logo marquee duplicate copy rendered as aria-hidden spans (was 2× keyboard stops + double announcements); error-boundary fallback announces via role="alert". Manual NVDA/VoiceOver walkthrough still open |
| WCAG accessibility pass (automated) | 🟢 axe-core e2e gate (`e2e/accessibility.spec.ts`): 22 light routes (404, bright-brand company, all 3 services pages, privacy/terms/affiliate-disclosure — legal templates were previously ungated, and privacy shipped a link-in-text-block violation: an inline "GitHub Issues" link distinguishable only by color, now underlined) + 14 dark routes. Theme-aware accent system: `--acc-0..8`, `--tool-acc-*`, `--accent-ink` tokens with light/dark twins in globals.css; tools hub + calculators suite + matchmaker + category pages consume vars (color-mix for alpha tints), so every accent is ≥4.5:1 as text and as chip/tab background in both themes; `.eyebrow` and always-visible card CTAs use `--accent-ink` (immune to inline brand-colour overrides); dark `--muted` brightened (#9a9488 → #a29c90, fixes 4.35:1 10px chip). Verified: axe CLEAN on 36 routes; 75/75 e2e, 317 vitest, budget 417,667/450,000 |
| Keyboard focus gate (2.4.1/2.4.7) | 🟢 `e2e/keyboard.spec.ts`: Tab-walks 7 templates asserting every stop shows a visible indicator, plus skip-link (Enter → focus lands in `#main-content`, now `tabIndex={-1}`) and keyboard-reachability of the primary nav. Root cause found: `ring-[var(--ring)]` is a no-op in Tailwind v4 (a bare var() is ambiguous length/colour) — 32 occurrences rendered no focus ring. Fixed with one unlayered `:focus-visible { outline: 2px solid var(--accent-strong); outline-offset: 2px }` rule (unlayered beats utility `outline-none`; `.btn-*` keep their shadow ring via specificity) |
| RSS autodiscovery link | 🟢 `<link rel="alternate" type="application/rss+xml" href="/feed.xml">` now in `<head>` (this Next fork does not emit metadata `alternates.types` in static exports — rendered literally). Verified in out/index.html |
| Service-worker cache auto-versioning | 🟢 `scripts/version-service-worker.mjs` (postbuild) stamps `out/sw.js` CACHE_NAME with a content hash of the `_next` assets — any asset change rotates the cache name so `activate` purges stale caches on deploy. Deterministic (rebuilds with unchanged assets keep the same name); manual "bump the release identifier" discipline removed. Negative-tested (missing `_next` → exit 1) |
| Dark-theme contrast audit | 🟢 Initial dark scan found 9 real violations (tools hub accents 2.2–2.7:1, calculators active tab 2.92:1, markup chip 4.35:1) — all fixed by the theme-aware accent tokens above. Dark pass of the a11y gate now guards every template |
| Provider-specific ops beyond [`deployment-providers.md`](../deployment-providers.md) | Ongoing |
| Dedicated Razorpay fee calculator page (`/tools/razorpay-fee-calculator`) | 🟢 Published-rate table + 18% GST math, reverse-charge formula, INR-preseeded estimator; Razorpay profile CTA updated |
| T049/T050/T051/T055 architecture wiring | 🟢 Every fee-related article links its calculator via a "Try the calculator" box (10/14 articles; receive-side omission closed by the new exchange-rate markup calculator); calculator pages link related comparisons; every company profile links the articles that mention it (server-side, hidden when empty); Article + BreadcrumbList JSON-LD on articles and profiles |
| India-first homepage directory + glossary | 🟢 Homepage "India-First Providers" curated order (Razorpay, Cashfree, Payoneer, Wise, PhonePe, Paytm); glossary +5 India terms (UPI, MDR, FIRC/FIRA, FEMA, T+1) wired into the related-term graph and category maps |
| Matchmaker quiz India coverage | 🟢 Quiz now recommends Indian providers: new "Freelancer / Independent" answer (international + transfers + multi-currency requirements), Cashfree capability entry, Razorpay capabilities extended (SaaS/enterprise, international, multi-currency, low-fee), Payoneer gains personal tier, PhonePe/Paytm score on personal/all-in-one; legacy SCORE_WEIGHTS matrix kept in sync. Verified: freelancer track → Wise/Razorpay/Revolut; SaaS+low-fee → Razorpay/Cashfree lead |
| Exchange-rate markup calculator (`/tools/exchange-rate-markup-calculator`) | 🟢 Input-only FX markup tool (no live-rate claims): mid vs offered rate → markup % + INR/USD loss for both USD→INR and INR→USD; reference corridors table (Payoneer 1–4%, Wise near-mid, ~0.5% balance conversions); share/save/URL-restore; wired as relatedTool on payoneer-fees-india, how-to-send-money-abroad-cheap, wise-vs-payoneer-business-payouts |
| Client bundle diet (company summaries split) | 🟢 Client components now import a generated client-safe summary catalog (`src/generated/company-summaries.ts`, ~9.7 KB gz) instead of the full editorial catalog (~32 KB gz); barrel-import leaks removed (compare-presets, partners, footer, palette, directory clients). JS budget 447,413 → 412,685 / 450,000 with the new tool added; drift gated by `company-summaries.test.ts` + prebuild regeneration |
| Tool discovery + cross-linking sweep | 🟢 Command palette now indexes all 6 tools (Razorpay fee + FX markup calculators were missing); homepage teaser gains the FX Markup button; remittance and matchmaker tool pages get "Related comparisons" sections (T050 pattern) — every tool page except the pure-client calculators suite now links its related articles/tools. Palette discovery e2e added (20/20) |
| Month-3 article 17 (Receiving $500 from a US client in India) | 🟢 Shipped 2026-08-04: worked ₹-landed table across Wise / Payoneer / bank wire / PayPal at the estimator's ₹83.50/USD snapshot (≈ ₹41,529 vs ≈ ₹37,080 — >10% spread between channels), FIRC + FEMA + Section 44ADA notes (editorial, not tax advice), scenario picks, remittance + FX-markup calculator links. Distinct calculations per the plan's "no near-identical amount pages" rule. RSS 14 items, sitemap 88 URLs, 266 JSON-LD blocks |
| Month-3 article 23 (International payment settlement times) | 🟢 Shipped 2026-08-04: 8-rail timeline table (UPI/IMPS instant, NEFT ~30 min, RTGS real-time, gateway T+1 standard with T+0 caveats + merchant-hold/refund dynamics, Wise minutes–hours, Payoneer 1–2 days, SWIFT 2–5 days with correspondent-chain explanation), business-scenario picks, tool cross-links. RSS 15 items, sitemap 89 URLs, 270 JSON-LD blocks, budget 421,601/450,000 |
| Month-3 article 25 (FIRA vs FIRC payment-method comparison) | 🟢 Shipped 2026-08-04: FIRA (advice, auto-issued) vs FIRC (formal certificate, on request) comparison table, when the certificate matters (GST export claims, DTAA documentation), how to request one (netbanking/branch, free), payment-method FIRC treatment table — SWIFT always, Wise/Payoneer bank-dependent, PayPal typically none (domestic settlement from the Indian entity) — with record-keeping guidance. Matches the glossary's combined "FIRC / FIRA" term. RSS 16 items, sitemap 90 URLs, 274 JSON-LD blocks, budget 422,855/450,000 |
| Month-3 article 26 (Quarterly India Cross-Border Payment Fee Index) | 🟢 Shipped 2026-08-04: consolidated USD→INR index ($500/$1,000/$5,000 × Wise/Revolut/Payoneer/PayPal/bank wire), every cell computed from the FX estimator's provider configs + ₹83.50 snapshot — with a drift test (`src/lib/fee-index.test.ts`, 3 tests) that recomputes all published ₹ figures from `remittance-config` and fails if article or config changes without the other. Same pass found + fixed a real cross-site inconsistency: the receiving-$500 article used a Wise fee curve and ₹86 vintage that matched neither the estimator's model (0.43% + $0.50 → $2.65 at $500, not $5.10) nor its ₹83.50 snapshot; payoneer-fees-india math realigned to ₹83.50 too. RSS 17 items, sitemap 91 URLs, 278 JSON-LD blocks, vitest 320 (38 files), budget 424,134/450,000 |
| FX-consistency sweep (articles vs estimator config) | 🟢 All USD→INR worked examples now derive from the same fee models + ₹83.50/USD snapshot as the Cross-Border FX Estimator: article 17 (₹41,529 / ₹40,915 / ₹39,887 / ₹37,080 at $500), payoneer-fees-india (₹82,665 at 1%, ₹80,160 at 4%), fee index (12 config-derived cells). Revolut/Wise crossover at ≈ $714 documented. Drift-gated by fee-index.test.ts |
| Month-3 article 20 (Payment gateway for Indian subscription businesses) | 🟢 Shipped 2026-08-04: fills the B2B merchant gap (prior run was freelancer/FX-heavy) — UPI AutoPay mandate rails as the deciding factor for Indian subscriptions, card-on-file eMandates, eNACH (3–7-day setup, Stripe India without it — hedged), capability table across Razorpay/Cashfree/Stripe India, dunning/proration/GST-invoice billing features, pick-by-customer-mix scenarios, fee calculator cross-link. All three gateways' published 2% + GST domestic fee consistent with the fee calculator. RSS 18 items, sitemap 92 URLs, 282 JSON-LD blocks, budget 425,879/450,000 |
| Month-3 article 19 (Receiving $5,000 from a US client in India) | 🟢 Shipped 2026-08-04: distinct from article 17 by design — at $5,000 the channel gap compresses to ~3% (vs >10% at $500, computed from the index), so the article's core is EEFC accounts (holding USD receipts at an AD bank, eligibility/limits/currency-risk hedged), FIRC + invoice-pair documentation, advance-payment/milestone mechanics, and a pick list (Wise/ACH for direct clients, Payoneer for platform rails, never a wire at this size). $5,000 column values drift-gated by fee-index.test.ts (now 4 tests, 321 total). RSS 19 items, sitemap 93 URLs, 286 JSON-LD blocks, budget 427,507/450,000 |
| Month-3 article 21 (Razorpay vs Stripe for developers) | 🟢 Shipped 2026-08-04: developer-DX angle distinct from the business-level razorpay-vs-stripe article (verified: that one covers fees/settlement/methods only) — API design (Stripe idempotency/versioning vs Razorpay's India resource surface), SDK/plugin coverage, webhook event models, test tooling (Stripe test clock as the recurring-billing differentiator), PCI scope (SAQ-A guidance), India-specific rails (UPI intents, AutoPay mandates, RazorpayX payouts), build-on decision matrix. All capabilities hedged to the 2026 catalog vintage. RSS 20 items, sitemap 94 URLs, 290 JSON-LD blocks, budget 429,670/450,000 |
| Month-3 article 22 (Razorpay vs Cashfree for ecommerce) | 🟢 Shipped 2026-08-04: ecommerce-operations angle distinct from the general razorpay-vs-cashfree comparison (verified: that one covers fee schedules and headline differences only) — checkout/store plugin depth, EMI + pay-later coverage caveats, COD reconciliation and refund/dispute loops, international ecommerce (Razorpay International ~3% vs Cashfree from 2.95%, merchant-of-record alternative), store-type decision matrix. Terms hedged to the 2026 catalog vintage + merchant agreement variance. RSS 21 items, sitemap 95 URLs, 294 JSON-LD blocks, budget 431,286/450,000 |
| Month-3 article 18 (Receiving $1,000 from a US client in India) | 🟢 Shipped 2026-08-04 — completes the $500/$1,000/$5,000 trilogy (17/18/19) and closes Month-3 (8/8 shipped). Distinct angle: the freelancer-milestone bridge — at $1,000 the flat-fee structure (dominant at $500) and percentage structure (dominant at $5,000) balance out (~7% best-to-worst spread), so the decisions are platform rails (Upwork/Fiverr → Payoneer corridor), Wise USD balance as withdrawal alternative, and starting the FIRC/invoice-pair + 44ADA documentation habit. $1,000 cells drift-gated by fee-index.test.ts (5 tests, 322 total). RSS 22 items, sitemap 96 URLs, 298 JSON-LD blocks, budget 432,554/450,000 |
| Cross-page consistency sweep (FX corridors, footer nav, compare page) | 🟢 Bank-wire FX corridor aligned across remittance tool (4.5% baseline) and markup-page corridor table (1.5–4.5%); footer tools column now lists all 6 tools (Razorpay fee + FX markup were missing); `/compare` gains a "Related comparisons" section (T050 pattern). Verified: typecheck, lint, 308 vitest, 22/22 e2e, SITE_URL build (413,997/450,000), browser smoke |
| E2E math integration coverage | 🟢 Remittance e2e now asserts the ranking contract (Wise leads the default $1,000 model with 0% markup and $4.80 fee; PayPal shows 3.5%) plus the semantic breadcrumb; calculators suite e2e locks all 9 tabs and the SIP→EMI panel switch. Suite: 22/22 |
| A12 (changelog feed) | 🟢 Shipped 2026-08-04: `/changelog` page (19 curated, commit-verifiable entries: articles 17–26, markup calculator, services track, FX sweep, SEO/a11y/keyboard passes, guards, correction reporting, breadcrumbs) + `changelog.xml` RSS feed (19 items, generated in the postbuild chain, autodiscovered on the changelog page via `alternates.types`), footer Explore link, sitemap auto-includes the route (97 URLs). `changelog.test.ts` (4 tests) gates kinds, descending dates, valid dates, unique (href,title) pairs, and real internal routes; vitest 326 (39 files) |
| Latent title double-suffix (articles index + new changelog page) | 🟢 Found via the new postbuild `check-titles.mjs` audit: `/articles/` and `/changelog/` metadata titles repeated the site name that the layout template appends (rendered "X — FinTech Atlas — FinTech Atlas") — the earlier SEO pass checked lengths only, missing suffix duplication. Fixed both pages; audit now enforces: non-empty titles, ≤65 chars, no double suffix, suffix present on every non-root route (the root page renders its title verbatim — Next.js does not apply the root layout's template to the root page — and its 56-char title cannot fit the suffix; documented exemption). Same pass caught article 20's title at 66 chars (post-SEO-pass addition): retitled "Payment gateways for Indian subscriptions" (56). 100 pages audited in postbuild |
| Dead eyebrow color utilities (4 tool pages) | 🟢 `text-[var(--accent)]`/`text-[var(--muted-text)]` on `.eyebrow` spans in matchmaker (Initial Shortlist), calculators (Results), fee estimator (Recommendation), remittance (Maximum Received) were dead: `.eyebrow` sets `color: var(--accent-ink)` unlayered, which beats layered Tailwind utilities (verified via computed style: rgb(22,86,61) accent-ink rendered). Fixed per the site's established `!text-` convention (compare-client/about): three muted-intent labels now render muted (verified rgb(92,87,78)); the matchmaker accent class removed (raw `--accent` is the anti-pattern `--accent-ink` exists to avoid — WCAG-unsafe on small caps) |
| Orphan company profiles → article backlinks | 🟢 7 more profiles now genuinely linked from articles (19→29 of 42): article 23 gains a 9th rail row "Card rails (Visa Direct / Mastercard Send — minutes, where supported)" + related profiles paytm/phonepe/google-pay (India's UPI rails); neobank article gains "Beyond the big four" section (starling/nubank/bunq, updatedAt bumped); coinbase-vs-robinhood gains global-exchange paragraph (binance/okx). 2 orphans remained — card payments: apple-pay/braintree — closed in row 108 by best-payment-gateway-shopify-india (braintree as the third-party gateway option, apple-pay as the buyer-side wallet). Zero orphan profiles remain (42/42) |
| Article 18 spread claim precision | 🟢 "roughly ₹6,100, about 7%" → "about ₹6,150 — roughly 7.4%" (exact: ₹83,099 − ₹76,952 = ₹6,147 = 7.40%) to stay exactly recomputable from the drift-gated table |
| Non-finite input guards (NaN/Infinity) | 🟢 CALCULATOR_TEST_MATRIX.md audit found overclaims: fee-calculator, remittance, investment-calculators silently propagated NaN (guards checked only `<= 0`, which NaN passes). Fixed at the source: all investment computes return `null` via `isPositive`/`isNonNegative` helpers, `requiredSip`/`emergencyFundNeeded` return 0, fee-calculator + remittance throw TypeError on non-finite inputs (matching the cross-currency RangeError precedent), `computeNetWorth` throws TypeError. 5 new tests (308 total). Matrix cells now honest with no doc edits; clients validate inputs, so no UI behavior change. Verified: tsc, lint, 308 vitest, 22/22 e2e, SITE_URL build 413,997/450,000 |
| T057 (author + methodology links) | 🟢 Article pages now show a visible byline ("By FinTech Atlas" → `/about`) and a Methodology link → `/about#methodology`; the /about "Data Sources & Synthesizing Methodology" section gained the `methodology` anchor. Matches the JSON-LD Organization author. Verified: tsc, lint, 308 vitest, 22/22 e2e, SITE_URL build (414,006/450,000), browser smoke (byline + anchor render) |
| 104 | Internal article linking (T052) + template tables | 🟢 0 of 22 articles linked any other article from data — the plan's T052 gap. Added `relatedArticleSlugs` (curated, symmetric edges across 20 articles; affirm-vs-klarna and coinbase-vs-robinhood intentionally standalone) with a "Related guides" card grid on article pages (category + title + Read →, rendered only when non-empty, hidden when none) + a symmetry/self-reference validation test (327 vitest total). Same pass closed the last template gaps: capability-at-a-glance tables added to razorpay-vs-stripe-for-developers, razorpay-vs-cashfree-for-ecommerce, and a provider-type cost summary to how-to-send-money-abroad-cheap (all hedged, consistent with the estimator's models), and the fee-index article gained its missing relatedTool (→ remittance estimator). Three asymmetric edges caught by the new test and repaired. Verified: tsc, lint, 327 vitest, 75/75 e2e, SITE_URL build (budget 433,653/450,000, 100 pages, title audit pass), browser (cards render on dev article; guide table 5 rows) |
| 105 | Gap cycle: a11y gate extension, RFC 9116 security.txt, orphan closure (29→34) | 🟢 (1) A11y gate never covered the changelog template or the new article components (tables, Related guides grid) — axe-probed 7 candidate routes clean, then added changelog + receiving-$5,000 + fee-index to the light pass and changelog + receiving-$5,000 to the dark pass (e2e 75→80). (2) No security.txt existed anywhere in the artifact; postbuild security-headers script now emits `/.well-known/security.txt` (RFC 9116: Contact = GitHub issues inbox, Expires, Policy, Canonical from SITE_URL) — the existing static-artifact checker's Contact+Policy validation caught the first draft missing Policy. (3) best-neobanks-2026 had no relatedTool (fee-index was fixed last cycle; BNPL/crypto have no matching calculator — intentional). (4) 2 new articles close 5 orphan profiles: "Gusto vs ADP vs Paychex for US payroll" (gusto/adp/paychex; contractor-payment India angle, capability table, W-2 vs 1099 classification risk hedged) and "Brex vs Relay: business banking for startups" (brex/relay; VC-backed vs bootstrapped pick, US-entity caveat) — symmetric edges wired (receiving-500/1000 ↔ payroll, neobanks/best-gateway ↔ brex, payroll ↔ brex); changelog +2 (21 entries); a latent duplicate edge slug on receiving-500 deduped. 8 orphans remain (venmo, cash-app, mercado-pago, picpay, plaid, apple-pay, braintree, circle). Verified: tsc, lint, 327 vitest, 80/80 e2e, SITE_URL build (budget 435,758/450,000, sitemap 99, RSS 24+21, 308 JSON-LD/104 files, title audit 102 pages, security.txt served) |
| 106 | Gap cycle: Payroll + Business Banking + stablecoin + P2P articles, editorial recency sort (orphan closure 34→37 of 42) | 🟢 (1) Four new articles, two per plan cadence: gusto-vs-adp-vs-paychex-us-payroll (contractor-payment angle for Indian companies), brex-vs-relay-business-banking (VC-backed vs bootstrapped fit), stablecoins-for-cross-border-payments (USDC vs bank rails: 3-leg on/off-ramp mechanics, capability table vs Wise/Payoneer/wire, Section 115BBH/194S + FIRC/FEMA caveats, drift-safe hedged numbers), cash-app-vs-venmo (US P2P fee table incl. business rates; the India angle: US-only, no INR, frozen-balance workarounds). Orphan closure 34→37 of 42; 5 remain (mercado-pago/picpay, plaid, apple-pay/braintree). (2) Recency ordering is now editorial everywhere: homepage "Latest Guides & Comparisons", /articles index, and RSS sort by updatedAt desc with array-order tiebreak (later array index first among equals); e2e asserts the top-3 set plus the positional order contract. (3) 8 new symmetric related-article edges with genuine body mentions: settlement-times gains a 10th rail row "Stablecoin rails (USDC)", fee-index "what's not in the index" exclusion bullet, receiving-$500/$5,000 P2P + USDC warnings, neobanks + send-money bridges. The symmetry test caught a missed back-edge (how-to-send-money-abroad-cheap) mid-cycle; a stale-anchor edit briefly duplicated relatedArticleSlugs/publishedAt before repair. Changelog: 2 entries (tag #473B). Metrics: RSS 26 items, sitemap 101 URLs, 316 JSON-LD blocks, budget 439,762/450,000, titles audit 104 pages, vitest 327/327, e2e 80/80, SITE_URL build ✓, SW stamp fintech-atlas-d2740f34c0 |
| 107 | Gap cycle: LatAm payments + open-banking articles, T015 named author, orphan closure (37→40) | 🟢 (1) Two new articles, two per plan cadence: receiving-payments-from-latin-america-in-india ("How to get paid from Latin America in India" — PIX/Mercado Pago/PicPay corridors stop at the border; the channels that actually reach an Indian bank account, incl. Wise BRL account details and Payoneer marketplace payouts, hedged) closes mercado-pago/picpay; plaid-vs-indias-account-aggregator ("Plaid vs India's Account Aggregator" — the US bank-data API model vs the RBI's consent-based NBCC-AA framework, Sahamati, live since 2021) closes plaid. Orphan closure 37→40 of 42; 2 remain (apple-pay, braintree — card payments). (2) T015 named author: every article now carries `author: "FinTech Atlas editorial team"` and the JSON-LD author renders as a Person (was Organization-only byline). (3) 4 new symmetric related-article edges with genuine body mentions: best-neobanks-2026 gains a Plaid-vs-AA paragraph, cash-app-vs-venmo an India-angle data-sharing bullet, receiving-$500 a corridor closing sentence, stablecoins an Argentina (high-inflation corridor) bullet. Changelog: 2 entries (tag #AF54). Metrics: RSS 28 items, sitemap 103 URLs, 324 JSON-LD blocks across 108 HTML files, budget 443,677/450,000, titles audit 106 pages, vitest 327/327, e2e 80/80, SITE_URL build ✓, SW stamp fintech-atlas-506fc14773 |
---
| 108 | Gap cycle: T009 ownership audit + plan items 5 & 8 + orphan closure (40→42) | 🟢 (1) T009 complete: every company profile carries a machine-checkable ownership classification (public/private/subsidiary/division/acquired) rendered as a quick-stat with an accessible one-line hint. The audit corrected four stale statuses — Klarna publicly listed (NYSE since 2025-09-10), Afterpay operates inside Block (ASX source label retired), MoneyGram private since the 2023 Madison Dearborn take-private, Cash App reclassified division (was subsidiary) and its "Square's" copy updated to Block — each now backed by a provenance record with an ownership support tag (whitelist extended) and drift-gated by provenance.test.ts (3 new tests). The WIP edit that had deleted the SectionHeader component (syntax error) was repaired and the barrel export added. (2) Two plan-mandated articles shipped: best-payment-gateway-shopify-india (plan item 8 — Shopify Payments is not available in India, so the decision is third-party gateway + Shopify's own third-party fee; genuinely links BOTH remaining orphan profiles: braintree as the API-first third-party option and apple-pay as the buyer-side wallet not yet available in India) and payment-gateway-fee-comparison-india (plan item 5 — fee anatomy + published India schedules consistent with the fee calculator config: 2% + 18% GST = 2.36% effective, intl surcharges to 3.53–3.54%, GST worked math, hidden costs; distinct from the two-way head-to-heads). Orphan closure 40→42 of 42 — zero orphan profiles remain. Reciprocal related-article links added on 6 existing articles; changelog +2 entries (30 total) |
| 109 | Gap cycle: plan items 13 & 14 (Upwork/Fiverr payout guides) + client-bundle split (budget headroom) + regressions | 🟢 (1) The two remaining Month-2 freelancer articles shipped: best-payment-method-upwork-india (service fee 0–15% locked per contract, typically 10%; Direct to Local Bank $0.99 / ≤4 business days / $5 min vs Payoneer's 1–4% corridor vs the free-ACH Wise two-step; $1,000 worked example; FIRC notes) and best-payment-method-fiverr-india (flat 20% incl. tips; 14-day/7-day clearance; official payout table — PayPal $0, bank transfer via Payoneer $1/$20 min, Payoneer account $3/$10 min, Revenue Card discontinued Dec 2022; $5,000/withdrawal cap; USD-only). Both web-verified against the official Upwork/Fiverr help centers (2026-08-04) and consistent with the fee-index models; homepage top-3 e2e contract updated (Fiverr → Upwork → fee comparison). (2) Client-bundle split: the command palette imported the full article catalog (every body in the client JS); new scripts/generate-article-summaries.ts derives a client-safe slug/title/description/category/relatedCompanySlugs subset (prebuild + drift test, mirroring the company-summaries pattern). Budget: 447,840 → 410,062 gzip (≈38 KB headroom for future content; 39 assets). (3) Regressions found and fixed: robinhood's ownershipType: "public" had been stripped from the working tree (build-breaking TS2741; restored — NASDAQ: HOOD since 2021, consistent with the T009 audit) and a misdirected reciprocal-link edit had landed on receiving-$500's relatedArticleSlugs (restored; links correctly added to receiving-$1,000). Verified: tsc, 334 vitest, eslint, SITE_URL build green (107 sitemap URLs, RSS 32, changelog RSS 31, 340 JSON-LD, 112 HTML, titles 110 pages), e2e 80/80, HTTP smoke on both new articles (200, Article + BreadcrumbList JSON-LD) |
| 111 | Gap cycle: per-page meta CSP + S13 issue mirror + LHCI errors-in-console fix | 🟢 (1) The CSP from the previous cycle shipped inside the document as a per-page `<meta http-equiv="Content-Security-Policy">` tag (hash-allowlisted to each page's own executable inline scripts; ~0.3–2.3KB per page vs a discarded ~60KB catch-all `_headers` union) — host-agnostic, GitHub Pages included. This cycle removed `frame-ancestors 'none'` from the meta policy: CSP3 ignores it in `<meta>` and Chrome logs a console error for it, which broke the `errors-in-console` LHCI assertion on all 6 gate URLs. Clickjacking protection remains via `X-Frame-Options: DENY` in `_headers` (the guard: deployment.test.ts asserts the meta CSP contains no `frame-ancestors 'none'`). (2) S13 done: 10 operator/decision gaps mirrored into GitHub issues (#12 deploy-block, #13 GitHub settings, #14 Search Console, #15 analytics, #16 uptime DEPLOYMENT_URL, #17 pentest + legal, #18 manual UX audits, #19 rollback drill, #20 affiliate/newsletter, #21 live CWV). Verified: tsc, 337 vitest (41 files), lint, e2e 80/80, SITE_URL build green (110 sitemap URLs, RSS 34+34, 115 HTML, 352 JSON-LD, titles 113, SW stamp fintech-atlas-3428751d43), LHCI full gauntlet GREEN (errors-in-console 0 across all 6 URLs, LCP/TBT/CLS/category assertions pass) |



## 4. Architecture & product surface

| ID | Gap |
|----|-----|
| A4–A5 | Partner / monetization helpers exist but are unused; all relationships `"none"` |
| A6 | Waitlist endpoint stub — no UI until `NEXT_PUBLIC_WAITLIST_ENDPOINT` + product decision |
| A10 | Affiliate playbook exists; enrollment, legal review, and live-link verification remain operator tasks |
| A11 | No i18n (`lang="en"` only) |
| A12 | No changelog feed (RSS feed now covers published articles) |

| T058–T062 | Services track | 🟢 `/services` shipped: gateway selection audit (₹999–₹1,999 basic / ₹2,500–₹5,000 detailed) + integration (₹3,000–₹15,000+) with deliverables/exclusions (T059), booking form (T060 — prefilled GitHub-issue channel, the site's only public inbox; mailto swap documented when an operator email exists), public sample audit report (T061 — fee table computed at build time from the SAME `PROVIDER_FEE_CONFIGS` the calculator uses, so rates can't drift; fictional merchant, real India schedules 2% + 18% GST), interactive implementation checklist (T062 — 5 phases, localStorage persistence, progress bar). Cross-linked from footer Explore, tools hub CTA, compare page, and each other. Service + OfferCatalog JSON-LD, breadcrumbs. Gated: 3 light + 2 dark axe routes, 1 keyboard route. Verified: 75/75 e2e, 317 vitest, budget 417,667/450,000, 262 JSON-LD/92 files; booking form falls back to a direct draft link when the popup is blocked (window.open result verified); PWA manifest shortcuts (fee estimator, services) |
| A14 | `"private": true` vs MIT LICENSE intent | 🟢 Intent documented — `"private": true` in `package.json` only prevents accidental publishing to the npm registry; the MIT `LICENSE` file is the governing license for the repo and is unchanged. Combined this is the conventional "source-available under MIT, never published as a package" setup. |

---

## 5. Data & content quality

| ID | Gap |
|----|-----|
| D1–D3 | Structured provenance is present for all 42 companies; legacy labels still require source-by-source re-verification |
| D4 | Logo asset coverage and branded fallback treatment still need an operator/content pass for any missing real marks |
| D5–D7 | Manual FX / fee / `DATA_AS_OF` / editorial refresh; fee calculator now isolates USD/INR and applies configured India GST, but source freshness remains manual |
| D8 | Possible editorial quality issues in company copy | 🟢 Catalog-wide copy sweep done: ~30 garbled fragments repaired across 25+ profiles (Payoneer, Paytm, PhonePe, Google Pay, Coinbase, Circle, Klarna, Gusto, MoneyGram, OKX, and others); empty user-review pros/cons filled from each entry's own editorial summary; Paytm/PhonePe verified in-browser |
| D9 | Illustrative fee/FX assumptions — freshness risk |
| D10 | On-device newsletter intent and private notes remain local-only UX by design |

---

## 6. WIP / engineering

| ID | Gap |
|----|-----|
| W3–W4 | E2E + security/Lighthouse workflows run on all pushed branches locally — merge to `origin/main` + enable GitHub settings |
| W7 | Dual lockfiles resolved (npm only) — ensure `pnpm-*` stay deleted |
| E7 | Structured-data and internal-link validation | 🟢 Done — `scripts/check-structured-data.mjs` (now also gates BreadcrumbList on every tool page) and `scripts/check-internal-links.mjs` run in `postbuild` and validate the emitted artifact; 266 JSON-LD blocks (BreadcrumbList on all 42 profiles + 14 articles + 7 tool routes incl. the /tools hub), internal-link checker covers 93 HTML files; checker also enforces per-type required properties (Article/BreadcrumbList/WebSite/Organization), gates BreadcrumbList on every tool route, and now validates anchor fragments too — every `href="#…"` and `/path#frag` must resolve to a real `id` in the target page (covers the ~180 glossary-term anchors, skip-links, and the article `/about#methodology` links; negative-tested on a mutated copy) |
| E11 | Audit triage policy thin |
| E15 | ESLint pinned to v9 until eslint-config-next supports 10 |
| E17 | Secret scanning push-protection still needs GitHub repo settings |
| E18 | Incident runbook recovery checklist remains an operator execution checklist; executable artifact/deployment checks are now documented |
| S5 | Glossary hash deep links are not separate sitemap URLs (by design for static anchors) |
| T013-adjacent | Breadcrumb consistency pass | 🟢 Shared `Breadcrumbs` component (visible nav + BreadcrumbList JSON-LD) now used on articles, company profiles, editorial tool pages, and the /tools hub; the four client-island tools gained "Home" links, semantic `<nav aria-label="Breadcrumb">` markup, and server-side BreadcrumbList JSON-LD; `check-structured-data.mjs` hardened with per-type required-property validation (negative-tested: missing BreadcrumbList + missing headline both fail) and `breadcrumbs.test.tsx` covers the component. Verified: 252 JSON-LD blocks, 308 vitest, 22/22 e2e, SITE_URL build 413,997/450,000 |
| S13 | Mirror high-priority gaps into GitHub Issues | 🟢 Done 2026-08-13 — 10 operator/decision issues opened (deploy-block #12, GitHub settings #13, Search Console #14, analytics #15, uptime DEPLOYMENT_URL #16, pentest/legal #17, manual UX audits #18, rollback drill #19, affiliate/newsletter #20, live CWV/SEO dashboard #21), each referencing EXECUTION_PLAN task IDs and the relevant docs |

---

## 7. Docs / legal / consistency

| ID | Gap |
|----|-----|
| L1 | Jurisdiction-specific legal review |
| C3 | Goals 11/13 vs privacy stance — mitigated by ADR-001; keep docs aligned |
| C8 | Enabling Plausible needs operator env + privacy already discloses optional analytics |

---

## 8. Local implementation boundary

The repository-level implementation pass is complete for the currently chosen
static architecture. The following remain intentionally open because they need
operator access, external review, or a new architecture decision: production
`SITE_URL` and host verification, GitHub settings, live CWV/indexability checks,
manual keyboard/screen-reader audit, penetration testing, affiliate/newsletter
provider enrollment, jurisdiction-specific legal review, rollback drills, and
all backend-heavy goals listed in ADR-001.

## 9. How to maintain

- Keep completed rows only when they provide useful verification context; mark
  them 🟢 and add the evidence source.
- Remove stale rows when the surrounding status section is rewritten.
- Backend ambitions require a new ADR superseding ADR-001.
